---
description: Fix linting errors
---

# Fix Lint

Fix all linting errors in the codebase by:
1. Run `pnpm typecheck:all` to identify all linting errors
2. Analyze the errors and their locations
3. Fix each error by editing the affected files
4. Run again to verify all errors are resolved
